server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        git:
          uri: https://github.com/dummy/repo # Placeholder, we use native for local dev
          default-label: main
  profiles:
    active: native
